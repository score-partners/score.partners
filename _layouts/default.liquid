<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>SCORE Partners</title>

  <style>
   {% include github-style.css %}

    body {
      font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
      margin: 0;
    }
    main, nav>ul {
      max-width: 1110px;
      margin-left: auto;
      margin-right: auto;
      padding: 0 8px;
      font-size: 16px;
    }

    main {
      display: flex;
      flex-direction: row;
    }
    section#content {
      width: 100%;
    }
    section#content article {
      padding: 32px;
    }
    
    /* General styling */
    a:link, a:visited, a:active {
      color: inherit;
    }
    a:hover {
      color: #039;
    }

    #edit-links {
      text-align: right;
      border-bottom: 1px solid #fc0;
    }

    #edit-links>a {
      display: inline-block;
      {% comment %} background: #eee; {% endcomment %}
      padding: 8px;
      text-decoration: none;
    }

    table {
      border-spacing: 0;
      border-collapse: collapse;
    }
  </style>
</head>
<body>
  {% include score-nav.html %}
  {% include header.html %}
  <main>
    {% include sidebar.liquid %}
    <section id="content">
    <div id="edit-links">
      <a href="https://github.com/score-partners/score.partners/issues/new?body=I'd like to report an issue in `{{ page.path }}`">⚠️ Report issue with this page</a>
      <a href="https://github.com/score-partners/score.partners/edit/master/{{ page.path }}">📝 Edit on GitHub</a>
    </div>
    <article>
      {{ content }}
    </article>
    </section>
  </main>
  {% include footer.html %}
</body>
</html>
